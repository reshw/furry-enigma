{
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "claude": "^0.1.1"
  },
  "devDependencies": {
    "vercel": "^44.0.0"
  }
}
